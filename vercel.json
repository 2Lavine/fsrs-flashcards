{
  "buildCommand": "pnpm build",
  "outputDirectory": "packages/client/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "functions": {
    "api/**/*.ts": {
      "includeFiles": "packages/llm-config/src/**"
    }
  }
}
