#!/bin/sh
# Pre-push hook: deploy to Vercel when pushing to main

current_branch=$(git rev-parse --abbrev-ref HEAD)
if [ "$current_branch" = "main" ]; then
  echo ""
  echo "→ Pushing to main. Deploying to Vercel..."
  echo ""
  pnpm deploy &
fi
